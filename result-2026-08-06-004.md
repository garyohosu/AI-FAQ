# result-2026-08-06-004

## 判定

`SUCCESS_WITH_LIMITATIONS`

instruction-2026-08-06-001〜004の必須要件は実装・テストしていますが、
Gemini CLIの実認証環境でのWeb調査「成功パス」の実地確認ができていない点、
および001が想定していた「AI調査結果を人間承認でナレッジ化するコマンド」が
004のCLI一覧に無いため未実装である点から、`SUCCESS_WITH_LIMITATIONS` と
判定しました。詳細は「残る制約と次の作業」を参照してください。

## 実施した作業

1. `git status --short` を確認(クリーン)、`git pull` 実行(1コミット取得済み、
   作業開始時点でfast-forward済み)。
2. instruction 001〜004を全文読み、矛盾点(コマンド名、`reply`の引数形式等)
   を洗い出し、004を優先しつつ非矛盾要件は001〜003からも取り込み。
3. Python/SQLite/FTS5/Gemini CLIの実環境を事前確認(advisor助言に従い、
   実装着手前にLangGraph/langgraph-checkpoint-sqliteの実APIをコード確認)。
4. 層ごとに実装(下記「実装した構成」参照)、各層を実装直後に手動スモーク
   テストで検証しながら積み上げ。
5. pytestテストスイートを作成し、全件成功・カバレッジ計測まで実施。
6. `knowledge/`・`memory/`・`MEMORY.md` の安全なサンプルを作成。
7. README・本resultファイルを作成。
8. 変更をコミットし、`origin/main` へpush。

## 実装した構成

```text
AI-FAQ/
├─ README.md
├─ MEMORY.md
├─ pyproject.toml
├─ .gitignore
├─ .env.example
├─ .gemini/settings.json
├─ src/aifaq/
│  ├─ __init__.py, __main__.py, cli.py, config.py, models.py
│  ├─ security.py, routing.py, graph.py
│  ├─ db.py, repositories.py, ingestion.py, memory.py, util.py
│  └─ providers/{base.py, fake.py, gemini_cli.py}
├─ tests/ (conftest.py + 9 テストファイル, 84テスト)
├─ knowledge/
│  ├─ README.md, public/*.md, public/*.csv (安全なサンプルのみコミット)
│  └─ internal/ policies/ procedures/ troubleshooting/ inventory/ inbox/
│     (README以外は.gitignore対象)
├─ memory/ (README.md, terminology.md, source-notes.md, decisions.md, gaps.md)
└─ instructions/, result-2026-08-06-00{1..4}.md
```

責務分離(001の要求層)は次の通り実装しました。

- CLI層: `cli.py` (argparse)
- LangGraphワークフロー層: `graph.py`, `routing.py`
- Pydanticモデル層: `models.py`
- SQLiteリポジトリ層: `db.py`, `repositories.py`
- 知識検索・取り込み層: `ingestion.py`
- メモリー索引層: `memory.py`
- セキュリティ・外部送信可否判定層: `security.py`
- AI CLIプロバイダー層: `providers/base.py`(Protocol), `fake.py`, `gemini_cli.py`

## 変更ファイル一覧

`git status --short`(コミット前点)は「最終 `git status --short`」章の通り、
新規追加のみです。主なファイルは上記ディレクトリ構成の通りです
(合計: srcモジュール17ファイル、testsファイル10ファイル、
knowledge/配下14ファイル、memory/配下5ファイル、その他設定・文書ファイル)。

## CLIコマンド一覧

```text
aifaq init [alias: init-db]
aifaq doctor
aifaq ask "質問" [--thread-id THREAD] [--requester NAME]
aifaq reply THREAD "回答"
aifaq history THREAD
aifaq pending list [--status OPEN|ANSWERED|CANCELLED]
aifaq pending show ID
aifaq pending answer ID --approved-by NAME [--by NAME(alias)] [--answer-file FILE]
    [--category C] [--tags a,b] [--variants a,b] [--valid-until ISO] [--reason R]
aifaq knowledge scan
aifaq knowledge import [PATH]
aifaq knowledge sync [PATH]        # importの別名(差分取り込み)
aifaq knowledge search "検索語" [--limit N]
aifaq knowledge list [--status DRAFT|APPROVED|RETIRED]
aifaq knowledge show ID
aifaq knowledge retire ID --by NAME
aifaq knowledge status
aifaq knowledge rebuild-index
aifaq memory validate
aifaq memory show
aifaq memory sync
```

全コマンドは `--json`(サブコマンドより前に指定)でJSON出力に対応します。
コマンド名の対応表(001/002 → 004)はREADMEに記載しました。

## DBとLangGraphの設計概要

### SQLiteテーブル

`knowledge_articles`, `knowledge_revisions`, `question_variants`,
`pending_questions`, `query_history`, `research_runs`,
`clarification_turns`(`UNIQUE(thread_id, round_no)`), `source_files`,
`source_chunks`, `source_import_runs`(スキーマのみ。件数記録は現状
`import_all`の戻り値経由で行っており、テーブルへの書き込みは未実装
= 既知の制約参照), `memory_entries`, `schema_meta`。

FTS5仮想テーブル: `knowledge_fts`, `source_fts`, `memory_fts`。
**`tokenize='trigram'`を採用**(既定のunicode61は日本語を単語分割できず、
部分一致検索が機能しなかったため、実装中に検証のうえ変更)。3文字未満の
検索語はLIKE検索へフォールバックします。FTS5が使えない環境では
`doctor`/`schema_meta.fts5_available`で検知し、全面的にLIKE検索へ
フォールバックします。

LangGraphのチェックポイントは業務データと同じ `data/aifaq.db` に
`SqliteSaver.from_conn_string()` で保存しています(ファイルは共有、
テーブル名は別)。

### LangGraphノード

```text
START → validate_question → security_precheck → load_memory_context
      → search_approved_knowledge → search_imported_sources
      → check_source_conflicts → assess_answerability
          ├─ knowledge → compose_knowledge_answer → record_history → END
          ├─ clarify  → create_clarification_question → ask_requester(interrupt)
          │              → merge_clarification → security_precheckへ戻る
          ├─ research → research_with_cli
          │              ├─ 十分 → compose_research_answer → record_history → END
          │              └─ 不十分/エラー → request_human(interrupt) → END
          └─ human    → request_human(interrupt) → END
```

`interrupt()`前の副作用(`clarification_turns`の作成、`pending_questions`の
作成)は冪等にしています(`(thread_id, round_no)`重複チェック、
`get_open_by_thread`による重複pending防止)。

## セキュリティ対策

- `security.py` の決定論的ルールで、パスワード等の秘密情報語・値、
  個人情報(メール/電話)、社内固有語(社内/弊社/申請先/工場等)、内部IP/
  内部ホスト名、重要操作語を検出し、`SECURITY_SENSITIVE`/`PERSONAL_DATA`/
  `INTERNAL` と分類した質問は `safe_for_external_research=False` を
  Pydanticバリデータで強制します(`PUBLIC_GENERAL`以外はTrueにできない)。
- `routing.decide_route` は `SECURITY_SENSITIVE` を検出した場合、確認質問
  すら生成せず即座に人間引き継ぎへ進みます(秘密情報の聞き返しを防止)。
- `research_with_cli` ノードは、`assess_answerability` が `research` を
  選んだ場合(=公開一般質問かつ`safe_for_external_research=True`の場合)
  にしか到達しません。テスト(`test_graph.py`)で内部/機密質問時に
  `FakeResearchProvider.call_count == 0` を明示的に検証しています。
- Gemini CLIは `subprocess.run(shell=False)`・引数配列・タイムアウト・
  stdout/stderrサイズ制限・JSON検証・`--yolo`不使用で呼び出します。
  `.gemini/settings.json` でWeb検索/Web取得以外のツールを無効化。
- 確認質問の回答は保存前に秘密情報らしき値を伏字化(`redact_for_storage`)。
- `knowledge/internal/` 等の実ファイルはGit管理対象外、`aifaq doctor` が
  誤追跡を警告。

## テストコマンドと結果

```bash
python -m pytest -q
# => 84 passed
python -m pytest -q --cov=aifaq --cov-report=term-missing
# => 84 passed, TOTAL coverage 84%
```

主な検証項目(instruction 001〜003のテスト要件に対応):

- Pydanticモデルの正常・異常入力、SQLite初期化冪等性
- 承認済み知識の完全一致・別表現・FTS5(trigram)検索優先
- 人間回答が承認知識として保存され、次回の類似質問で再利用される
- AI調査結果が自動でナレッジ化されない(`test_research_answer_never_auto_becomes_knowledge`)
- 確認質問が最大3回で止まり、4回目が生成されない
  (`test_three_rounds_max_no_fourth_question`)
- 「わからない」2回連続で人間引き継ぎ(`test_two_consecutive_unknown_escalates_before_round_limit`)
- 内部質問・秘密情報を含む質問がFakeProviderへ一切渡らない
  (`provider.call_count == 0` を明示アサート)
- 公開一般質問のみがFakeProviderへ渡る
- Excel(複数シート・非表示シート除外・xlsm)・CSV(UTF-8/CP932)・Markdown・
  TXTの取り込み、SHA-256差分取り込み、削除ファイルのMISSING化
- MEMORY.mdの正常解析・必須章欠落検出・重複ID拒否・`../`パス拒否・
  存在しない参照ファイルの警告
- `forbidden`資料が検索対象から除外される
- LangGraphの中断・再開(同一プロセス内、および**別プロセス起動**での
  再開を`test_thread_resumes_across_fresh_connections`で確認)
- CLIの主要経路(`ask`/`reply`/`pending answer`/`knowledge import`/
  `memory validate`をJSON出力込みで確認)

## カバレッジ

全体84%。コアロジック(graph.py 95%, routing.py 92%, security.py 95%,
models.py 95%, memory.py 90%, ingestion.py 88%, repositories.py 86%,
gemini_cli.py 92%)は80%以上を達成。`cli.py`は58%とやや低く(未網羅の
サブコマンド分岐: `knowledge show/retire/status/rebuild-index`等の一部)、
既知の制約として記載します。

## 実Gemini CLIで確認した内容、または未確認理由

確認できた内容:
- `gemini --version` → `0.50.0` で実行成功
- `gemini --help` でCLI引数仕様を確認(`-p/--prompt`, `-o/--output-format
  json`, `--approval-mode`, `--skip-trust` 等)
- 信頼されていないディレクトリでは `--skip-trust` 等が必須という
  trusted-folders仕様を実際のエラーメッセージで確認
- 上記を踏まえて `GeminiCLIProvider` を実装

未確認だった内容と理由:
- 実際のWeb調査を伴う**成功パス**は確認できていません。本環境の
  Gemini CLIアカウントが `IneligibleTierError`(利用ティア制限、
  "This client is no longer supported for Gemini Code Assist for
  individuals" )を返し、認証済みの問い合わせ自体ができませんでした。
  これは実装の不備ではなく、本環境のアカウント種別に起因する制約です。
- そのため、CLI全体のJSON包みの正確なキー名(`response`等)は
  公式ドキュメント記載の一般的パターンに基づく実装であり、実データでの
  最終確認ができていません。`GeminiCLIProvider._extract_app_json` は
  複数のキー候補・コードフェンス除去・1回のリトライに対応する形で
  防御的に実装し、`unittest.mock` によるユニットテストは実施済みです。
- 導入環境で認証済みのGemini CLIが利用できる場合、`aifaq ask` を
  `--fake-provider` を付けずに一度実行し、成功パスの出力形式を
  確認することを推奨します(README「既知の制約」にも記載)。

## 残る制約と次の作業

1. **Gemini CLI成功パスの実地確認**(上記参照)。認証可能な環境で
   `aifaq ask "<公開一般質問>"` を実行し、`GeminiCLIProvider` のJSON抽出
   ロジックが実際の出力形式と一致するか確認・必要なら調整する。
2. **AI調査結果のナレッジ昇格コマンド未実装**。instruction-001は
   `research approve <id>` 相当を想定していましたが、instruction-004の
   CLI一覧(完了条件の基準)には含まれていなかったため実装していません。
   必要であれば `aifaq research approve <research_run_id> --by NAME` を
   追加し、`research_runs` → `knowledge_articles(source_type=APPROVED_AI)`
   への昇格処理を実装できます。
3. **`source_import_runs` テーブルへの書き込み未実装**。スキーマは
   作成済みですが、`ingestion.import_all` の集計結果を実際にこの
   テーブルへ記録する処理は未実装です(戻り値としては取得できます)。
4. **`check_source_conflicts` は簡易実装**。同一検索結果内に
   `priority=authoritative` の資料が2件以上あり、内容の類似度が
   閾値未満の場合にのみ「矛盾」と判定する簡易ヒューリスティックです。
   意味的な矛盾検出ではありません。
5. **Python 3.12実行環境での確認未実施**。本環境にはPython 3.14.0のみ
   インストールされており、3.12/3.13では未検証です(3.12+互換の構文の
   みを使用しています)。
6. **`cli.py`のカバレッジがやや低い(58%)**。`knowledge show/retire/
   status/rebuild-index`等、一部サブコマンドの単体テストが未整備です。

## 最終コミットSHA

`6abdb4b5a25a74e32c6bc499943a93eddef100da`
(feat: implement learning AI FAQ MVP)

## push結果

成功。`origin/main` へpush済み(`f0f6d18..6abdb4b main -> main`)。

## 最終 `git status --short`

```text
(クリーン、差分なし)
```
