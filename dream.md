## 2026-08-06 16:40 Dreamingタイム

### 今回やったこと
- instruction-2026-08-06-001〜004を統合し、学習型AI FAQ MVPをゼロから実装
- Pydantic v2モデル、決定論的セキュリティ判定、SQLite(FTS5)リポジトリ層、
  Excel/CSV/TSV/Markdown/TXTの知識取り込み、MEMORY.md索引パーサー、
  LangGraph(確認質問ループ・interrupt/resume)、CLI、pytestテスト84件を実装
- knowledge/・memory/の安全なサンプルとREADMEを作成し、コミット・push

### 気づいたこと
- SQLite FTS5の既定トークナイザー(unicode61)は日本語を単語分割できず、
  部分一致検索が機能しなかった。`tokenize='trigram'`に切り替えることで
  日本語の部分一致検索が実用的になった(ただし3文字未満のクエリは
  ヒットしないため、短いクエリはLIKEへフォールバックする設計にした)。
- 本環境のGemini CLIは`IneligibleTierError`(利用ティア制限)により
  実際のWeb調査呼び出しの成功パスを確認できなかった。CLI仕様
  (`--help`、trusted-folders要件)は確認できたが、成功時のJSON出力形式は
  ドキュメントベースの推測実装にとどまる。
- 本環境にはPython 3.14.0のみが入っており、指示書が指定する3.12での
  検証はできなかった。

### 改善点
- `cli.py`のテストカバレッジが58%とやや低い。`knowledge show/retire/
  status/rebuild-index`等のサブコマンド単体テストを追加すると良い。
- `check_source_conflicts`(複数資料の矛盾検出)は類似度ベースの簡易実装。
  本格的な意味的矛盾検出には至っていない。
- `source_import_runs`テーブルへの実書き込みが未実装(スキーマのみ)。

### 次に試すとよさそうなこと
- 認証可能なGemini CLI環境で`aifaq ask`を実際に1回実行し、
  `GeminiCLIProvider._extract_app_json`が実データと整合するか確認する
- AI調査結果を人間承認でナレッジ化する`research approve`コマンドの追加
  (instruction-001が想定していたが、instruction-004のCLI一覧には
  含まれておらず今回は未実装とした)
- Python 3.12/3.13環境での動作確認

---

## 2026-08-06 (instruction-2026-08-06-005: Antigravity移行)

### やったこと
- Gemini CLI前提から Antigravity CLI (`agy`) へ移行。`AntigravityProvider`
  を実装し、`GeminiCLIProvider` と `.gemini/settings.json` を削除
- 環境変数を `AIFAQ_RESEARCH_*` へ統一(旧 `AIFAQ_GEMINI_BIN` は非推奨警告)
- `aifaq research list/show/approve/reject` を追加(AI調査結果の人間承認)
- `source_import_runs` の実書き込み、スキーマv2への冪等マイグレーション
- GitHub Actions CI (Python 3.12 / 3.13、FakeProvider固定)
- テスト84件 → 193件、全体カバレッジ84% → 90%、cli.py 58% → 83%

### 気づいたこと
- **推測で実装しないことが本当に効いた**。過去の `werewolf-game` の
  `config/agents.json` と `OracleCouncil` の `adapters/agy.py` に、
  `agy --print "<prompt>"` という実績のある呼び出し方式が残っていた。
  さらに実機で `agy --help` を確認したところ、OracleCouncil実装時
  (1.1.5/1.1.6)には無かった `--output-format json` が 1.1.9 で追加されて
  おり、生テキストではなくエンベロープを前提にできた。過去資産の確認と
  実機確認の両方をやって初めて正しい実装になった。
- **agyの `status: "ERROR"` は「全体の失敗」ではなかった**。調査中に1件でも
  ツール呼び出しが失敗すると、最終回答が完全に得られていても `status` が
  `ERROR` になる。最初は `status != SUCCESS` で機械的に失敗にしていたため、
  使える調査結果を丸ごと捨てて人間回答待ちへ送っていた。本文が取り出せる
  限り採用し、`error` の内容を警告として人間に見せる形に直した。
  実機で一度動かさなければ絶対に気づけなかった。
- **検索を良くしたらセキュリティの穴が開いた**。質問文をそのままFTS5の
  1フレーズとして検索していたため、完全文の質問はほぼ必ず0件になり、
  取り込み資料へ到達できていなかった。語を抽出してOR検索するよう直した
  ところ、今度は機密質問(「社内サーバーの管理者パスワード」)が
  `knowledge/README.md` に一致してしまい、**人間引き継ぎを飛ばして
  資料の中身を回答してしまった**。`decide_route` が取り込み資料の一致を
  引き継ぎ判定より先に見ていたのが原因。検索精度の向上が、それまで
  死んでいた危険な経路を目覚めさせた形で、機能改善が安全性を壊しうる
  という良い教訓になった。
- **日本語の語分割は助詞で切ってはいけない**。最初は助詞(は/が/を/の…)を
  区切り文字にしたが、「繋がらない」が「が」で割れて「らない」という
  無意味な語になった。漢字・カタカナ・英数字の連なりを内容語として取り、
  ひらがなは文法要素として捨てる方式に変えたら一気に安定した。
- **関連度の分母を間違えると閾値が効かない**。一致率をFTS5へ渡せる3文字
  以上の語だけで測っていたため、短い語が分母から消えて過大評価され、
  見出しの「Windows」ひとつで無関係な資料を拾い続けた。単体テストは
  通るのに実機で再現するという厄介な形で出た。関連度は必ず抽出した
  全検索語に対して測る。

### 改善点
- 検索の関連度判定(文字数比0.4)はヒューリスティックで、閾値の根拠は
  手元の事例のみ。資料が増えたら再調整が要る。
- `AIFAQ_RESEARCH_TRANSPORT=file`(参照渡し)は実機未検証のまま残した。
  agy側の `permissions.allow` 設定が必要で、利用者の全体設定を勝手に
  変えない判断をしたため。

### 次に試すとよさそうなこと
- agy の `permissions.allow` を使った参照渡しの実機検証
- 検索の関連度閾値を、実データを増やして再評価する
- 既存サービス層を呼ぶ薄いWeb UI(instruction-005 §6の次段階)

---

## 2026-08-06 (instruction-2026-08-06-006: 人間回答ループの完成)

### やったこと
- `aifaq status` / `aifaq watch` / `aifaq chat` を追加
- `pending answer` が元のpendingとthreadへ人間回答を保存するようにした
- DBスキーマv3(既存DBを削除しない冪等な列追加)
- SQLiteのWAL・busy_timeout・上限付きロック再試行
- 2ターミナル運用を別プロセスで実機検証、subprocess統合テストも追加
- テスト193件 → 242件、カバレッジ90% → 91%

### 気づいたこと
- **「回答を保存した」と「質問者に届いた」は別物だった**。指示書が
  `delivery_status` / `delivered_at` を分けて要求していた意味が、実装して
  みてよく分かった。statusを叩くたびに状態が変わったり回答が消えたりしたら
  ヘルプデスクとして使えない。取得は何度でもでき、初回受領時刻だけを
  記録する形に落ち着いた。
- **二重回答の防止はSELECT→UPDATEでは足りない**。最初は「取得して状態を
  確認してから更新」と書いたが、2ターミナルから同時に叩かれると両方とも
  チェックを通過してしまう。`UPDATE ... WHERE id=? AND status='OPEN'` の
  更新件数で判定する形に変えた。別プロセス2つを本当に同時起動するテストを
  書いたら、片方だけが成功することを確認できた。机上で考えるより、
  実際に2プロセス走らせたほうが早かった。
- **PRAGMAは接続ごと**。WALはDBファイルに永続するが `busy_timeout` は
  接続ごとなので、`connect()` で必ず設定する必要がある。片方だけ設定して
  「効かない」と悩みかけた。
- **watchがDBを掴みっぱなしにしてはいけない**。ポーリングのたびに接続を
  開いて短い読み取りで閉じる形にした。長時間1接続を保持すると、書き手の
  `pending answer` を待たせる可能性がある。
- **進捗表示の出力先を間違えると`--json`が壊れる**。「待機中...」を標準出力へ
  出していたらJSONが混ざって解析できなくなる。標準エラーへ出すことで、
  `aifaq --json watch | jq` がそのまま通るようにした。指示書が
  「進捗表示でJSONを壊さない」と明記していた理由が実感できた。
- **PowerShellのパイプはBOMを付ける**。`echo "..." | aifaq pending answer` が
  「制御文字が含まれています」で弾かれた。自分で入れた検証が自分の手順を
  止めたわけだが、これは検証が正しく働いた証拠でもある。ファイル読み込み側で
  既にutf-8-sigにしていたので、stdin側も同じ扱いに揃えた。
- **既存の例外型を変えると静かに壊れる**。`AlreadyAnsweredError` を
  `RuntimeError` 継承で作ったら、`ValueError` を期待していた既存テストが
  落ちた。新しい例外を足すときは、既存の呼び出し側が何を捕まえているかを
  先に見るべきだった。`ValueError` 継承に直して互換性を保った。

### 改善点
- 未登録の言い換えでは承認済み知識が再利用されない(スコア0.5 < 閾値0.55)。
  005で意図的に入れた安全側の設計なので、勝手に緩めず未達として報告した。
  緩めるなら一致率に応じた可変スコアが要る。
- `cli.py` が800行を超えてきた。status/watch/chatは別モジュールへ分けた
  ほうが見通しが良い。

### 次に試すとよさそうなこと
- 一致率に応じた知識スコアの可変化(要判断)
- note記事(実機結果は揃っている)
- 薄いWeb UI。サービス層はそのまま使える
