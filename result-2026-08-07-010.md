# result-2026-08-07-010

## 結論

`aifaq chat`が回答完了後も同じ`thread_id`を再利用していたため、次の質問で表示された確認質問とSQLite上の確認状態が食い違う不具合を修正した。

## 原因

従来の対話モードは、起動時に生成した1つの`thread_id`をセッション終了まで使い続けていた。

LangGraphの`thread_id`は「1件の元質問と、その質問に対する最大3回の確認」の単位である。最終回答済みのチェックポイントへ別の質問を重ねたことで、画面上では確認質問が表示されても、`aifaq reply`相当の処理が参照する未回答確認レコードが存在しない状態になった。

## 変更内容

- `src/aifaq/chat_session.py`
  - 最終回答後の次の入力で新しい`thread_id`を生成
  - 確認質問中だけ同じ`thread_id`を維持
  - `/status`は現在または直前の有効なthreadを参照
  - `ReplyError`発生時は入力を捨てず、新しい質問として自動復旧
- `src/aifaq/entrypoint.py`
  - 既存CLIのコマンド定義を維持しつつ、`chat`だけ修正版へ差し替え
- `src/aifaq/__main__.py`
  - `python -m aifaq`も統合エントリーポイントを使用
- `pyproject.toml`
  - バージョンを`0.1.2`へ更新
  - `aifaq`コマンドを`aifaq.entrypoint:main`へ変更
- `start-aifaq.ps1`
  - `.exe`ではなくPythonモジュールを直接起動
  - editable install環境で`git pull`後のソースを直接反映
- `tests/test_chat_session.py`
  - 最終回答後に新しいthreadへ切り替わるテスト
  - 確認状態が失われた場合の自動復旧テスト

## 想定される修正後の動作

```text
あなた> プリンタが繋がりません
AI-FAQ> ...最終回答...

あなた> 良くわかりません
AI-FAQ> 新しい質問を開始します。thread_id: <新しいID>
AI-FAQ> どの部分が分かりませんか？

あなた> 最初に何をするのかわかりません
AI-FAQ> ...回答または次の確認質問...
```

`未回答の確認質問が見つかりません`で行き止まりになる代わりに、確認処理を継続できる。

## 検証

- 通常のthread切替をモックした回帰テストを追加した。
- `ReplyError`時に入力を新しい質問として再投入する回帰テストを追加した。
- GitHub上のソース、エントリーポイント、ランチャーの接続関係を静的確認した。
- Windows実機での最終動作確認は、利用端末で`git pull`後に行う必要がある。

## 判定

`RESULT_PRODUCED_WITH_LIMITATIONS`

実装修正は完了。制限は、この環境からWindows実機とAntigravity CLIを組み合わせた実会話を実行できない点である。
