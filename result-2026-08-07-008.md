# result-2026-08-07-008

## 結論

AI-FAQの起動処理を自動化した。今後は、仮想環境を手動で有効化せず、管理者用または質問者用のCMDを実行するだけで起動できる。

## 追加ファイル

- `start-aifaq.ps1`
  - リポジトリへ自動移動
  - `.venv`の自動作成
  - 必要時だけeditable installを実行
  - 管理者、質問者、doctor、メニューを起動
  - `-Update`でgit pullと再インストール
  - `-Install`で再インストール
- `start-admin.cmd`
  - IT管理者画面を起動
- `start-chat.cmd`
  - 質問者チャットを起動
- `start-aifaq.cmd`
  - 起動メニューを表示
- `START_GUIDE.md`
  - 日常操作と更新方法を記載

## 日常操作

IT管理者:

```powershell
C:\PROJECT\AI-FAQ\start-admin.cmd
```

質問者:

```powershell
C:\PROJECT\AI-FAQ\start-chat.cmd
```

メニュー:

```powershell
C:\PROJECT\AI-FAQ\start-aifaq.cmd
```

## 設計上の注意

- 通常起動では毎回`pip install`しない。
- `.venv`またはCLI実行ファイルが無い場合だけ自動セットアップする。
- PowerShellスクリプト内で`exit`を使わず、呼び出し元のPowerShellを終了させない。
- 更新時に別プロセスが`aifaq.exe`を使用しているとWindowsのファイルロックで失敗するため、`-Update`または`-Install`の前に既存のAI-FAQ画面を終了する。

## 検証

- ファイル構成とPowerShell/CMDの呼び出し関係を静的確認した。
- GitHub上へmainブランチとして反映した。
- Windows実機での起動確認は、利用端末で`git pull`後に実施する必要がある。

## 判定

`RESULT_PRODUCED_WITH_LIMITATIONS`

実装は完了。制限はWindows実機での最終起動確認をこの環境から実行できない点のみ。
