# result-2026-08-06-005

## 判定

`SUCCESS_WITH_LIMITATIONS`

instruction-2026-08-06-005 の完了条件はすべて満たしています。実Antigravity
によるWeb調査の成功パスも実機で確認済みです。ただし §2.4 が求める
「一時ファイルへ調査指示を書き、短い参照指示で渡す」参照渡し方式が、
実環境の制約により**実機未検証**のまま残っているため、`SUCCESS` ではなく
`SUCCESS_WITH_LIMITATIONS` としました(詳細は「残る制約」参照)。

## 実施した作業

1. `git status --short`(クリーン)と `git pull --ff-only origin main` を実行。
2. `result-2026-08-06-004.md`、`hikitsugi.md`、現行のProvider抽象・設定・
   doctor・README・テストを確認。
3. **§2.1 の最重要方針に従い、Antigravityの呼び出し方式を推測せず調査**
   (下記「Antigravityの確認方法と結果」)。
4. `AntigravityProvider` を実装し、`GeminiCLIProvider` /
   `.gemini/settings.json` / `tests/test_gemini_cli_provider.py` を削除。
5. 環境変数を `AIFAQ_RESEARCH_*` へ統一(旧名は非推奨警告付きで継続読み取り)。
6. `aifaq research list/show/approve/reject` を実装(§5.1)。
7. `source_import_runs` の実書き込みを実装(§5.2)。スキーマv2への
   冪等マイグレーションを追加。
8. GitHub Actions CI(Python 3.12 / 3.13、FakeProvider固定)を追加(§5.3)。
9. テストを追加(§5.4)。84件 → 193件。
10. 実機スモークテストとCLI一周検証(§3, §4)を実施。
11. 検証中に見つかった不具合3件を修正(下記「検証中に発見・修正した不具合」)。
12. README / MEMORY.md / memory/*.md / dream.md を更新し、本resultを作成。

## Antigravityの確認方法と結果

### 過去の実装の調査(§2.1)

推測実装を避けるため、まず過去に稼働した実装を探しました。

| 参照元 | 確認できた内容 |
|---|---|
| `C:/project/werewolf-game/config/agents.json` | `{"command": "agy", "args": ["--print"], "prompt_mode": "arg"}` |
| `C:/project/OracleCouncil/src/oracle_council/adapters/agy.py` | `agy --print "<prompt>"`。プロンプトはargv渡し。1.1.5/1.1.6には stdin も `--prompt-file` も無いことを実測で確認済みとのコメントあり。Windowsのコマンドライン長上限(約32,767 UTF-16コード単位)の実測値も記録されていた |

両者とも**プロンプトをコマンドライン引数で渡す方式**でした。

### 実機確認(agy 1.1.9 → 作業中に 1.1.10 へ自動更新)

| 確認項目 | 結果 |
|---|---|
| `agy --version` | `1.1.9`(後に `1.1.10`) |
| `agy --help` | `--print` / `--output-format (text,json,stream-json)` / `--json-schema` / `--model` / `--add-dir` / `--dangerously-skip-permissions` を確認 |
| `agy --print "<prompt>"` | 終了コード0。stdoutは生テキスト |
| `agy --print "<prompt>" --output-format json` | 終了コード0。**エンベロープを返す**(下記) |
| Web検索(権限フラグなし) | **成功**。出典URL付きの回答を取得 |
| `--json-schema <file>` | 受理され、エンベロープにも反映される |
| ファイル読み取り(権限フラグなし) | **自動拒否**(下記) |

`--output-format json` のエンベロープ(実測):

```json
{"conversation_id":"...","status":"SUCCESS","response":"<本文>",
 "duration_seconds":2.5,"num_turns":1,"usage":{...}}
```

OracleCouncil実装時(1.1.5/1.1.6)にはこのエンベロープが無く生テキスト
だったため、**過去実装の確認だけでは不十分**でした。実機確認を併用した
結果、エンベロープを前提とした実装にできました。

ファイル読み取りを試みた際の実測メッセージ:

```
jetski: no output produced — a tool required the "read_file" permission that
headless mode cannot prompt for, so it was auto-denied. Add an allow-rule under
permissions.allow in settings.json (e.g. read_file(<target>)). Alternatively,
re-run with --dangerously-skip-permissions to auto-approve all tools.
```

### §2.1 と §2.4 の衝突と、その扱い

- §2.1 は「過去に動作した方式(= argv渡し)を再利用せよ」
- §2.4 は「質問全文を引数へ連結せず、一時ファイル参照渡しにせよ」

実環境では参照渡しに agy 側の権限設定が必要で、両立できませんでした。
利用者へ確認し、**「両方実装し、既定は実機確認済みの argv 方式」**を
選択いただきました。`AIFAQ_RESEARCH_TRANSPORT=arg|file` で切り替えます。

なお `--dangerously-skip-permissions` を用いた検証は、本セッションの
権限分類器にブロックされたため実施していません(回避策は取っていません)。

## 実機スモークテスト結果

| 項目 | 内容 |
|---|---|
| 実行日時 | 2026-08-06 |
| Antigravityバージョン | 1.1.9 / 1.1.10 |
| 使用コマンド形式 | `agy --print "<調査指示>" --output-format json` |
| 質問 | 「Windows 11でネットワークアダプターを再起動する公式手順は？」ほか |
| 終了コード | 0 |
| 出力形式 | JSONエンベロープ → `response` 内にアプリJSON |
| 取得できた出典 | `support.microsoft.com` / `learn.microsoft.com` の公式ページ |
| タイムアウト | なし(実測 約68〜105秒。既定タイムアウト180秒) |
| 結果 | **成功** |

追加で「Windows 11のディスク空き容量を確認する公式手順は？」
「Windows 11でBluetoothデバイスをペアリングする公式手順は？」でも
出典付き回答の取得に成功しました。

### 重要な実測所見: `status: "ERROR"` は全体failureではない

初回スモークテストは失敗扱いになりました。原因は、調査中に**1件でも**
ツール呼び出し(URL取得)が失敗すると、最終回答が完全に得られていても
`status` が `ERROR` になり、失敗内容が `error` に入るためでした。

```json
{"status":"ERROR","response":"<完全なJSON回答>",
 "error":"Failed to fetch document content at https://..."}
```

当初の実装は `status != SUCCESS` で機械的に失敗させていたため、使える
調査結果を捨てて人間回答待ちへ送っていました。**本文が取り出せる限り
回答を捨てず、`error` の内容を警告として回答に添えて人間に見せる**形へ
修正しました(§2.5「実環境で確認された揺れだけを安全に正規化する」)。
本文が空・壊れている場合は従来どおり失敗させます。

## CLI一周テスト結果(§4)

すべて実機で確認しました。「外部呼び出しなし」の検証は、
`AIFAQ_RESEARCH_BIN` を存在しない実行ファイル名に差し替えた状態で
正常応答することをもって確認しています。

| § | 項目 | 結果 |
|---|---|---|
| 4.1 | 承認済みFAQ登録 → 類似質問 → 外部を呼ばず回答、知識ID・版・出典を表示 | OK(`参照: KB-1`、`aifaq knowledge show 1` で version/source_urls/approved_by を確認) |
| 4.2 | 最大3回の確認質問、4回目は生成しない | OK(3/3まで質問後、4回目は「確認質問の上限に達したため人間対応が必要」で引き継ぎ) |
| 4.3 | 公開一般質問 → Antigravity呼び出し → 出典付き暫定回答、自動保存しない | OK(`review_status=PENDING` のまま。承認するまでナレッジ化されない) |
| 4.4 | 内部/機密質問 → Antigravityを一度も呼ばない | OK(`research_runs` の件数が **0件** であることをDBで直接確認) |
| 4.5 | 人間回答待ち → IT管理者が回答・承認 → SQLite保存 → 次回再利用 | OK(`KB-1` として保存、別表現「VPNパスワード再発行」で再利用を確認) |
| 4.6 | Excel/CSV/MD取り込み、MEMORY.md索引、ファイル・シート・行範囲の出典表示、差分取り込み | OK(下記) |

§4.6 の出典表示(実出力):

```
参照: knowledge/public/sample-troubleshooting.xlsx / シート「プリンター」 / 行2-2
参照: knowledge/public/windows-network-troubleshooting.md / 見出し「ネットワークアダプターを再起動する」 / 行6-11
参照: knowledge/public/general-it-glossary.csv / 行2-2
```

差分取り込み(実出力): 新規Excel追加時に
`検出10 追加1 更新0 不変9 欠落0 失敗0` となり、変更のないファイルが
`skipped_unchanged` になることを確認しました。

## 検証中に発見・修正した不具合

実機検証を通じて、既存実装の不具合3件を発見し修正しました。

### 1. 自然文の質問で取り込み資料に到達できない(致命的)

`SourceChunkRepository.search` / `KnowledgeRepository.search` が、質問文
**全体**をFTS5の1フレーズ(`"..."`)として検索していました。文がそのまま
資料に現れることはまずないため、完全文の質問はほぼ必ず0件になり、
**取り込み済み資料が事実上使えない**状態でした。

- 「スプーラー」→ 1件ヒット
- 「スプーラーの状態」→ 1件ヒット
- 「スプーラーの状態を確認する方法」→ **0件**

段階的フォールバックへ変更しました。

1. クエリ全体のフレーズ検索(最も精度が高い。完全一致を優先)
2. 抽出した検索語のOR検索(FTS5、3文字以上の語)
3. 抽出した検索語のLIKE検索(2文字語・FTS5非対応環境)
4. クエリ全体のLIKE検索(最後の手段)

検索語の抽出(`util.extract_search_terms`)は形態素解析器を使わず、
漢字・カタカナ・英数字の連なりを内容語として取り、ひらがなを文法要素
として捨てます。当初は助詞(は/が/を/の…)を区切り文字にしましたが、
「繋がらない」が「が」で割れて「らない」になるため採用しませんでした。

OR検索は語を1つ含むだけで当たるため、足切りを入れています。
「特徴的な語(3文字以上)を1つ以上」または「2語以上」を含み、かつ
一致語が質問の検索語全体の**文字数比で0.4以上**を占めることを要求します。

### 2. 機密質問が取り込み資料で回答されてしまう(セキュリティ)

不具合1を直した結果、検索が実際に当たるようになり、それまで到達不能
だった経路が露出しました。「社内サーバーの管理者パスワードを教えて
ください」が `knowledge/README.md`(フォルダ説明文書)に一致し、
**人間引き継ぎを飛ばして資料の中身を回答**しました。

原因は `routing.decide_route` の判定順序です。取り込み資料の一致を
引き継ぎ判定(`check_escalation`)**より先**に見ていました。

取り込み資料は「人間が回答として承認したもの」ではなく、ファイルの
中身をそのまま返すだけなので、引き継ぎ判定を先に行うよう順序を
変更しました。人間が承認した知識(`knowledge_articles`)は従来どおり
機密質問にも回答してよい扱いのままです(それ自体が承認済みの運用
ルールであり、学習ループの成果物にあたるため)。

あわせて `knowledge/**/README.md` を取り込み対象から除外しました。
フォルダの使い方を説明するリポジトリの付属文書であり、FAQの回答根拠
ではないためです。

### 3. 関連度の分母が誤っており閾値が効かない

関連度をFTS5へ渡せる3文字以上の語だけで測っていたため、短い語が分母
から消えて過大評価されていました。「Windows 11のディスク空き容量を
確認する公式手順は？」が、見出しの「Windows」ひとつだけの一致で
無関係な資料を拾い続けました(単体テストは通るのに実機で再現)。
関連度は必ず抽出した**全検索語**に対して測るよう修正しました。

### その他の小修正

- `--answer-file` を `utf-8-sig` で読むよう変更。Windowsのエディタが
  付けるBOMが回答本文の先頭に混入していました(実機で混入を確認)。
- `knowledge search` の出典表示にシート名を追加。
- Markdown/TXTの出典に見出しと行範囲を併記(従来はどちらか一方)。

## 自動テスト結果とカバレッジ

```bash
python -m pytest -q
# => 193 passed

python -m pytest -q --cov=aifaq --cov-report=term-missing
# => 193 passed, TOTAL 90%
```

| 指標 | 004時点 | 005時点 | 目標 |
|---|---|---|---|
| テスト件数 | 84 | **193** | 84件維持+追加 |
| 全体カバレッジ | 84% | **90%** | 80%以上 |
| `cli.py` | 58% | **83%** | 可能なら70%以上 |
| `providers/antigravity.py` | — | 89% | — |
| `repositories.py` | 86% | 91% | — |
| `util.py` | 100% | 97% | — |

§5.4 が要求したテストはすべて追加済みです。

- Antigravity出力の正常・異常・空出力(`tests/test_antigravity_provider.py` 32件)
- タイムアウト、非ゼロ終了コード、認証エラー、権限自動拒否、
  コマンドライン長超過、不正URL除去、部分失敗時の警告付き採用
- research approveの正常系・二重承認・修正版・却下・期限切れ・不正ID
  (`tests/test_research_approve.py` 14件)
- source_import_runs記録(`tests/test_import_runs.py` 7件)
- doctorのAntigravity確認、設定の互換性・不正値
  (`tests/test_config_and_doctor.py` 14件)
- 不正ID、DBロック時の明確なエラー(`tests/test_cli_subcommands.py` 16件)
- 日本語検索語抽出と段階的フォールバック検索
  (`tests/test_search_terms.py` 35件)

検索まわりのテストは、ご指示の観点をすべて含みます。

- 完全文の質問でExcel/CSV/Markdown/TXTが見つかる
- 助詞・疑問表現が含まれていても検索できる
- 完全一致・承認済み知識の優先順位を壊さない
- 無関係な資料を過剰に拾わない
- FTS5非対応環境のLIKEフォールバックでも動く
- 出典にファイル名・Excelシート・行範囲が付く

## Python 3.12/3.13 CI結果

`.github/workflows/ci.yml` を追加しました(matrix: 3.12 / 3.13)。
`AIFAQ_RESEARCH_PROVIDER=fake` を設定し、**`agy` が入っていないことを
明示的に検証**したうえでテストします(実AI CLIをCIから呼ばない担保)。
カバレッジは `--cov-fail-under=80` でゲートします。

ローカルでも両方を実機確認しました。

```text
Python 3.12.10 → 193 passed
Python 3.13.1  → 193 passed
```

CIと同じ `AIFAQ_RESEARCH_PROVIDER=fake` 環境でも両方193件成功します。
この検証中に、環境変数の漏れで壊れるテストを1件発見して修正しました
(CIが全体に `fake` を設定するため、分岐を明示選択する必要があった)。

## セキュリティ検証結果

- 内部/機密質問で `research_runs` が **0件**(外部プロバイダー未呼び出し)を
  DBで直接確認。
- 自動テストでも `FakeResearchProvider.call_count == 0` を明示検証(既存)。
- 機密質問が取り込み資料で回答されてしまう不具合を発見・修正
  (上記「検証中に発見・修正した不具合 2」)。回帰テストを追加済み。
- Antigravityへは質問文のみを渡し、`knowledge/internal/`・`memory/`・
  SQLite DB・認証情報・内部URLは一切渡しません。
- `subprocess.run(shell=False)` と引数配列のみを使用。
  `--dangerously-skip-permissions` は使用しません。
- stdout/stderrのサイズ制限、タイムアウト、終了コード検証、
  Pydanticによる出力検証、URL形式検証(スキーム・netloc)を実施。
  形式不正のURLは黙って捨て、出典0件なら人間回答待ちへ送ります。
- `agy` の不明な出力形式を推測で成功扱いにしません
  (実機で観測した崩れたJSON `{ok: true}` を成功にしないテストあり)。

## research approveの動作結果

実機で確認しました。

```text
$ aifaq research list
#1 [PENDING] status=ok conf=1.0 出典1件 : Windows 11でBluetoothデバイスを…

$ aifaq research approve 1 --approved-by hantani --category hardware --reason "Microsoft公式手順と一致"
承認済み知識として保存しました: KB-2 (research #1 APPROVED, 修正=なし)

$ aifaq research approve 1 --approved-by other
research run #1 は既に APPROVED です(二重承認はできません)     # exit=2
```

- 元のAI回答(`answer`)と出典(`sources_json`)は書き換えずに保持。
- 承認者・承認日時・修正有無・変更理由・昇格先ナレッジIDを記録。
- 承認時に `knowledge_articles`(`source_type=APPROVED_AI`)と
  `knowledge_revisions` へ保存し、出典URLを引き継ぐ。
- 二重承認は `UPDATE ... WHERE review_status='PENDING'` の更新件数0で
  検出して防止(競合時は作成済みナレッジをretireして巻き戻す)。
- 却下(`REJECTED`)・期限切れ(`EXPIRED`)も管理可能。
- 調査失敗した run は `NOT_APPLICABLE` となり承認対象外。

## source_import_runsの記録結果

実機で記録されることを確認しました。

```text
{'id': 1, 'target_path': 'knowledge', 'actor': 'hantani', 'detected': 3,
 'added': 3, 'updated': 0, 'skipped_unchanged': 0, 'missing': 0,
 'failed': 0, 'succeeded': 1}
```

§5.2 が要求した項目をすべて保存します(開始・終了日時、対象パス、
実行者、検出・追加・更新・スキップ・欠落・失敗件数、エラー概要、成否)。
取り込み中に例外が出た場合も `finish()` を必ず呼び、失敗として記録します。

`added` と `updated` を区別するため `FileImportOutcome.was_existing` を
追加しました。

## note記事を作成したか、作成しなかった理由

**作成していません。**

§7.1 の作成条件のうち、次を満たしています。

- 実Antigravityによる公開情報調査が成功 ✅
- 最大3回の確認質問が動作 ✅
- 人間回答を承認し、次回再利用できた ✅
- Excel・CSV・MarkdownまたはTXTの知識を検索できた ✅
- 内部質問で外部Providerが呼ばれないことを確認 ✅
- 全テスト成功 ✅

条件自体はすべて満たしていますが、本セッションでは検索不具合と
セキュリティ順序の修正が追加作業として発生し、その検証・テスト整備を
優先しました。記事は「実際に確認できていない機能を『動いた』と書かない」
(§7.5)ことが前提の成果物であり、今回修正した検索・ルーティングの
挙動を落ち着いて再確認したうえで書くべきと判断しています。

未達を隠さないため、**記事は未作成**として明記します。次回の作業で
`note/README.md` と `note/learning-ai-faq-for-beginners.md` を作成する
のが適切です(§7.3/§7.4 の構成はそのまま使えます)。

## 変更ファイル一覧

追加:

```text
.github/workflows/ci.yml
src/aifaq/providers/antigravity.py
tests/test_antigravity_provider.py
tests/test_research_approve.py
tests/test_import_runs.py
tests/test_config_and_doctor.py
tests/test_cli_subcommands.py
tests/test_search_terms.py
knowledge/public/sample-troubleshooting.xlsx
result-2026-08-06-005.md
```

削除:

```text
.gemini/settings.json
src/aifaq/providers/gemini_cli.py
tests/test_gemini_cli_provider.py
```

変更:

```text
src/aifaq/config.py         # AIFAQ_RESEARCH_* へ統一、旧名は非推奨警告
src/aifaq/cli.py            # provider選択・doctor・research系・import記録・DBロック
src/aifaq/db.py             # schema v2 + 冪等マイグレーション
src/aifaq/graph.py          # research_runs へ回答本文を保存
src/aifaq/ingestion.py      # was_existing、README除外
src/aifaq/repositories.py   # 段階的フォールバック検索、research承認、import runs
src/aifaq/routing.py        # 引き継ぎ判定を取り込み資料より先に
src/aifaq/models.py         # 出典表示に見出しと行範囲を併記
src/aifaq/util.py           # 日本語検索語抽出と関連度判定
README.md / MEMORY.md / memory/README.md / memory/decisions.md
memory/gaps.md / dream.md
```

## 残る制約

1. **参照渡し(`AIFAQ_RESEARCH_TRANSPORT=file`)が実機未検証**。agyの
   ヘッドレス実行が `read_file` を自動拒否するため、agy側
   `settings.json` の `permissions.allow` 設定が必要です。利用者の
   agy全体設定を勝手に変更しない判断をしたため未検証のまま残しました。
   実装・単体テストは完了しています(質問文がargvに載らないこと、
   一時ディレクトリが成否問わず削除されることを検証済み)。
2. **検索の関連度判定はヒューリスティック**。文字数比0.4という閾値の
   根拠は手元の事例のみで、資料が増えたら再調整が必要です。
3. **ひらがなのみの語は検索語にならない**。「ぱそこん」のような表記は
   `question_variants` で吸収する運用になります。
4. **`check_source_conflicts` は簡易実装**(004から変更なし)。意味的な
   矛盾検出ではありません。
5. **note記事は未作成**(上記の理由)。
6. **Web UIは未着手**。§6のとおり今回は対象外です。

## 最終コミットSHA

(このファイルを含むコミット後に追記)

## push結果

(push後に追記)

## 最終 `git status --short`

(コミット後に確認)
