# instruction-2026-08-07-009

## 目的

AI-FAQのREADME.mdを現在の実装へ合わせて更新し、初めて使う人が起動から日常操作まで迷わない状態にする。

## 必須対応

1. README冒頭にWindows向けの簡単起動手順を記載する。
2. 次の起動方法を明記する。
   - `start-admin.cmd`
   - `start-chat.cmd`
   - `start-aifaq.cmd`
   - `start-aifaq.ps1 -Update`
3. IT管理者の日常操作は`aifaq-admin`を主手順とする。
4. 質問者の`aifaq chat`、`ask`、`reply`、`status`、`watch`を説明する。
5. 手動セットアップ、Antigravity CLI、知識取り込み、AI調査結果の承認、MEMORY.md、テストを記載する。
6. `WinError 32`、`dubious ownership`、PowerShellの日本語パイプ問題の対処を記載する。
7. 既存実装と矛盾する古い操作を主手順にしない。
8. 変更後はmainへ反映し、結果を`result-2026-08-07-009.md`へ記録する。
