# result-2026-08-06-006

## 1. 判定

`SUCCESS_WITH_LIMITATIONS`

instruction-2026-08-06-006 の完了条件(§13)はすべて満たしています。
2ターミナルでの人間回答ループは実機で動作を確認しました。

`SUCCESS` にしなかった理由は1点です。§10の確認事項のうち
**「類似質問を新しいthreadで行うと承認済み知識から回答される」が、
未登録の言い換えでは成立しません**(完全一致と `--variants` 登録済みの
別表現では成立します)。詳細は「11. 未達事項」を参照してください。

## 2. 実装したコマンド

| コマンド | 内容 |
|---|---|
| `aifaq status THREAD_ID` | 状態と回答を一度だけ表示 |
| `aifaq status --pending-id 12` | 受付番号で検索 |
| `aifaq watch THREAD_ID` | 回答が入るまでポーリングして待つ |
| `aifaq watch --pending-id 12` | 受付番号で待つ |
| `aifaq chat --requester 名前` | 確認質問を繰り返す対話モード(§4.4) |

`aifaq --json status ...` / `aifaq --json watch ...` に対応しています。

### `status` が扱う状態

`NEEDS_CLARIFICATION` / `PENDING_HUMAN` / `ANSWERED` / `CANCELLED` /
`COMPLETED` / `NOT_FOUND` の6種類を `ThreadState` として定義しました。

出力にはthread_id、受付番号、元の質問、状態、確認質問履歴の要約、
回答本文、回答者、受付日時、回答日時、回答種別、承認済み知識ID、出典を
含みます。

### `watch` の仕様

- 既定2.0秒間隔(`--interval` で変更、`AIFAQ_WATCH_INTERVAL` でも設定可)
- 最短0.5秒に制限。下回ると終了コード2で明確にエラー
- `--timeout SECONDS` 指定可(省略時は無制限)
- 状態が変わったときだけ表示し、待機中に大量出力しない
- 進捗表示は**標準エラー出力**へ出すため、`--json` の標準出力は壊れない
- Ctrl+C で安全に終了(状態は壊さない)

終了コードはREADMEに定義しました。

| 終了コード | 意味 |
|---|---|
| `0` | 確定状態(回答済み・取り下げ・対応済み)を検知 |
| `1` | 対象が見つからない |
| `2` | 引数が不正(`--interval` が下限未満など) |
| `3` | `--timeout` に達した |
| `130` | Ctrl+Cで中止 |

### `aifaq chat`

既存の `run_ask` / `run_reply` をそのまま呼び、FAQロジックは重複実装して
いません。確認質問の最大3回制限もグラフ側の制御に従います。
`/status` で現在の状態表示、`/quit` で終了。人間引き継ぎ時は
「このまま回答を待ちますか？ [Y/n]」で待機/終了を選べます。

## 3. DBスキーマ変更

スキーマ版を `2` → `3` へ。`pending_questions` に次を追加しました。

| 列 | 型 | 用途 |
|---|---|---|
| `answer_text` | TEXT | 人間回答の本文 |
| `answer_type` | TEXT NOT NULL DEFAULT 'HUMAN' | 回答種別 |
| `delivery_status` | TEXT NOT NULL DEFAULT 'UNDELIVERED' | 質問者が受領したか |
| `delivered_at` | TEXT | 初回受領日時 |
| `updated_at` | TEXT | 更新日時 |

**既存の同等項目は重複追加していません。** `answered_by`、`answered_at`、
`resulting_knowledge_id`(§5の `knowledge_id` に相当)はv1から存在するため
そのまま使っています。

移行は `ALTER TABLE ... ADD COLUMN` による冪等な追加で、**既存DBを削除
しません**。`_ADDED_COLUMNS` に版ごとの追加列を宣言し、`PRAGMA table_info`
で未追加のものだけを足します。

## 4. 状態遷移

```text
OPEN / PENDING_HUMAN
  ├─ IT管理者が回答      → ANSWERED
  ├─ 管理者が取消        → CANCELLED
  └─ 未回答              → OPEN のまま

ANSWERED
  ├─ 質問者が status/watch で取得 → ANSWERED のまま(状態を壊さない)
  └─ delivered_at のみ更新(初回のみ。以降は上書きしない)
```

回答の「保存」と質問者が「読んだこと」を分離しています。受領しても
`status` は `ANSWERED` のままで、回答は消えず何度でも取得できます。

## 5. LangGraph再開の扱い

**チェックポイントは進めず、業務状態はSQLite側を正としました**(§4.3の8を
選択)。理由:

1. 再開するにはIT管理者側のプロセスが、質問者のグラフを
   `Command(resume=...)` で進めることになり、責務が逆転する。
2. グラフの再開を、業務状態の更新(pendingとknowledgeの保存)と同じ
   トランザクションに入れられない。片方だけ成功する状態が生まれる。
3. 質問者が `status` を叩くのは回答から数時間後かもしれず、そのとき
   グラフを再開しても得られるのはSQLiteに既にある回答である。

`status` / `watch` は `pending_questions` から回答を返します。
チェックポイントは中断したまま残りますが、`thread_id` は再利用されず
次の質問は新しいスレッドになるため実害はありません。
この判断はREADMEの「LangGraphのチェックポイントを進めない理由」にも
記載しました。

## 6. SQLite / WAL / busy_timeout の扱い

接続ごと(`db.connect`)に次を設定します。

- `PRAGMA journal_mode=WAL` — 質問者の `watch`(読み)がIT管理者の
  `pending answer`(書き)をブロックしない。WAL非対応FSでは例外を握って
  既定モードで継続します。
- `PRAGMA busy_timeout=5000`(`AIFAQ_BUSY_TIMEOUT_MS`)— PRAGMAは接続ごとの
  設定なので、接続を作る場所で必ず設定します。
- `PRAGMA synchronous=NORMAL` — WAL利用時の推奨値。ロック保持時間を短縮。
- `isolation_level="DEFERRED"` — 読み取りは自動コミットで短く終わり、
  `with conn:` の範囲だけが書き込みトランザクションになります。

`watch` は**ポーリングのたびに接続を開き、短い読み取りで閉じます**。
DBを長時間ロックしません。

ロック競合には `db.with_lock_retry()` で上限付き指数バックオフ再試行を
用意しました(既定5回)。**無限には再試行しません**。上限に達すると
`DatabaseBusyError` を送出し、CLIは生の「database is locked」ではなく
対処方法を含む日本語メッセージを表示します。ロック以外の
`OperationalError` は再試行せずそのまま送出します。

## 7. 2ターミナル実機テスト結果

同じフォルダで2プロセスを起動し、`AIFAQ_DB_PATH=data/test006.db` の
専用DBで実施しました(本番知識を汚さないため)。

| 確認事項(§10) | 結果 |
|---|---|
| ターミナルAのwatchが回答を検知する | OK(回答から **3.2秒**で検知、終了コード0) |
| 回答者と回答日時が表示される | OK(`回答者: hantani` / `回答日時: 2026-08-06T13:31:54...`) |
| `aifaq status` でも同じ回答を取得できる | OK |
| `aifaq history` に処理履歴が残る | OK(`PENDING_HUMAN` と `HUMAN_ANSWER` の2件) |
| 類似質問を新しいthreadで行うと承認済み知識から回答される | **部分的**(下記11参照) |
| DBロックエラーが発生しない | OK(発生なし) |

実際の出力(watch側):

```text
[IT管理者からの回答]
PC交換申請はIT管理担当へ提出してください。

受付番号: 1
thread_id: manual-human-001
質問: 社内のPC交換申請先を教えてください
受付日時: 2026-08-06T13:31:15.378126+00:00
回答者: hantani
回答日時: 2026-08-06T13:31:54.262668+00:00
回答種別: HUMAN
承認済み知識: KB-1
確認質問履歴:
  round1: Q=対象の手続き・申請は何ですか？ A=PC交換の申請です
```

進捗表示は標準エラーへ出ており、標準出力を汚していません。

```text
[stderr] 待機中... (状態: PENDING_HUMAN, 1.0秒ごとに確認。Ctrl+Cで終了)
```

二重回答も実機で確認しました。

```text
$ aifaq pending answer 1 --approved-by other
エラー: pending question 1 は既に ANSWERED です     # 終了コード 2
```

## 8. 自動テスト件数とカバレッジ

```bash
python -m pytest -q
# => 242 passed

python -m pytest -q --cov=aifaq --cov-report=term
# => 242 passed, TOTAL 91%
```

| 指標 | 005時点 | 006時点 |
|---|---|---|
| テスト件数 | 193 | **242** |
| 全体カバレッジ | 90% | **91%** |
| `cli.py` | 83% | 79%(chat/status/watch追加により分母が増加) |
| `repositories.py` | 92% | 92% |
| `db.py` | 92% | 90% |
| `security.py` | 96% | 96% |

Python **3.12.10 / 3.13.1 の両方**で242件成功を確認しました。

追加したテストファイル:

- `tests/test_human_answer_loop.py`(33件)— 人間回答ループ、status、
  watch、2接続、ロック再試行、回答本文の検証
- `tests/test_two_terminal_integration.py`(5件)— **subprocessで別プロセス
  起動**する統合テスト
- `tests/test_chat.py`(9件)— 対話モード
- `tests/test_config_and_doctor.py` に旧DB移行テストを2件追加

§9の要求項目はすべて網羅しています。

### 9.2(2接続・別プロセス)の実装

`test_two_terminal_integration.py` は実際に `subprocess` でCLIを起動します。

- `watch` を別プロセスで起動 → 別プロセスから `pending answer` →
  watchが検知して終了コード0(`test_watch_detects_answer_from_another_process`)
- `--json` でも標準出力がJSONとして解析できる
- 2プロセスが同時に同じpendingへ回答 → **片方だけ成功(0)、他方はエラー(2)**、
  既存回答は上書きされない
- 書き込みプロセスと並行して `status` を5回実行しても成功する(WAL)
- §10と同じ流れを別プロセスで通す end-to-end テスト

## 9. 旧DB移行テスト結果

`test_v2_database_gains_v3_pending_columns_without_data_loss` で、
v3の列を持たない旧 `pending_questions` を作って検証しました。

- v3の5列が追加される
- 既存の `answered_by` 等が重複追加されない
- **既存行が残る**(`original_question` が保持され、`status='OPEN'` のまま)
- 新列は既定値になる(`answer_text IS NULL`、`delivery_status='UNDELIVERED'`)
- 移行後の旧pendingへそのまま回答でき、`status` から取得できる

`test_migration_is_idempotent_across_repeated_init` で、`init_db` を
3回繰り返しても既存データが壊れないことも確認しています。

## 10. セキュリティ回帰テスト結果

§9.5の回帰項目はすべて維持されています(242件全成功)。

- 承認済み知識の優先 — 維持
- 日本語自然文検索(段階的フォールバック)— 維持
- FTS5なしのLIKEフォールバック — 維持
- 機密質問でProviderが呼ばれない — 維持
- 最大3回の確認質問 — 維持
- 「不明」2回で人間引き継ぎ — 維持
- Antigravityの出典付き公開情報調査 — 維持(`agy --version` 1.1.10 で
  doctor成功を確認)
- Excel・CSV・TSV・Markdown・TXT取り込み — 維持
- MEMORY.md検証 — 維持(`aifaq memory validate` OK)

今回追加したセキュリティ対応(§8):

- 人間回答は外部AIへ送信しません(SQLiteに保存するのみ)。
- `status` / `watch` は指定されたthread_id / 受付番号の情報だけを返します。
  値はSQLのプレースホルダーで渡し、SQL文字列へ連結していません。
- 回答本文を保存前に検証します(`security.validate_human_answer`)。
  空文字、上限20,000文字超過(`AIFAQ_MAX_ANSWER_CHARS`)、改行・タブ以外の
  制御文字を拒否します。実際に端末制御シーケンス `\x1b[31m` を拒否する
  テストを追加しました。
- JSON出力は `ThreadStatus` モデルの項目に限定しており、内部情報を
  追加で漏らしません。

認証がないことはREADMEに明記しました(下記11も参照)。

## 11. 未達事項

### 11.1 未登録の言い換えでは承認済み知識が再利用されない

§10の確認事項「類似質問を新しいthreadで行うと承認済み知識から回答される」
について、実測結果は次のとおりです。

| 質問 | スコア | 再利用 |
|---|---|---|
| 登録時と同じ文言 | 1.000 | ○ |
| `--variants` に登録した別表現 | 1.000 | ○ |
| 語がそのまま含まれる質問 | (全文検索) | ○ |
| 未登録の言い換え(例:「PC交換の申請先はどこですか」) | 0.500 | **×**(確認質問へ進む) |

閾値は `knowledge_match_threshold = 0.55` です。語のOR一致に 0.5 という
閾値未満のスコアを与えているのは、**instruction-005で意図的に入れた安全側の
設計**です(曖昧な一致だけで確認質問や人間引き継ぎを飛ばさないため。
005では取り込み資料の偶然の一致で機密質問が回答されてしまう不具合が
実際に発生しました)。

今回これを緩めると005のセキュリティ修正を巻き戻す恐れがあるため、
**指示外の変更として勝手に行わず、未達として報告します。** 運用上は
`--variants` によくある言い換えを登録することで対応でき、実機で
「PC交換の申請先はどこですか」「パソコン交換の申請先」がKB-1から
回答されることを確認済みです。READMEにも再利用が効く条件を表で明記しました。

必要であれば、語のOR一致スコアを一致率に応じて可変にする(例:
一致率が高いときだけ閾値を超える)対応が可能です。判断を仰ぎます。

### 11.2 その他

- `AIFAQ_RESEARCH_TRANSPORT=file`(参照渡し)は005から引き続き実機未検証です。
- `cli.py` のカバレッジが83% → 79%へ低下しました(chat/status/watchの追加で
  分母が増えたため)。目標の70%は上回っています。
- note記事(§12)は未作成です。§12の条件は満たしていますが、11.1の
  未達があるため「実際に確認できていない機能を動いたと書かない」方針に
  従い、記事化は次回作業としました。`hikitsugi.md` に記事化用の実機結果と
  コマンド例を残しています。

## 12. 変更ファイル一覧

追加:

```text
tests/test_human_answer_loop.py
tests/test_two_terminal_integration.py
tests/test_chat.py
result-2026-08-06-006.md
```

変更:

```text
src/aifaq/db.py            # schema v3、WAL/busy_timeout/synchronous、
                           # is_lock_error、DatabaseBusyError、with_lock_retry
src/aifaq/config.py        # busy_timeout_ms、watch_interval、max_answer_chars
src/aifaq/models.py        # ThreadState、ClarificationSummary、ThreadStatus
src/aifaq/repositories.py  # pending.answer拡張、mark_delivered、
                           # get_latest_by_thread、AlreadyAnsweredError、
                           # Repositories.thread_status
src/aifaq/security.py      # validate_human_answer、AnswerValidationError
src/aifaq/cli.py           # status/watch/chat、pending answer拡張、
                           # 引き継ぎメッセージ、stdin BOM除去
tests/test_config_and_doctor.py  # 旧DB移行テスト2件を追加
README.md                  # 2ターミナル運用、status/watch、認証範囲ほか
hikitsugi.md
```

## 13. 最終コミットSHA

`107385d` (feat: deliver human answers back to the questioner (status/watch/chat))

本ファイルへのSHA追記分は後続コミットになります。

## 14. push結果

成功。`origin/main` へpush済み(`a2de2a8..107385d`)。

## 15. 最終 `git status --short`

```text
(クリーン、差分なし)
```
